# THIS REPOSITORY CONTAINS DIFFERENT PATTERN DESIGN EXAMPLES
# TO RUN THEM IN CPP (WHICH IS THE MAIN LANGUAGE USED HERE)
# YOU NEED TO INSTALL cmake, 
# each project includes a CMakeLists.txt file

# CREATE A build folder in the project main route, example:
# at the same level of lib, src and the CMakeLists.txt
# the sequence of commands you may use is:
mkdir build
cd build
cmake ..
cmake --build .




